def execute(number):
    """
    Returns the analytical data for the main engine to display.
    No print() statements or console calls here.
    """
    
    # You can keep your logic here. 
    # Just format the data as a clean string and return it.
    
    analysis_text = (
        "Behavioral Score: [bold yellow]74/100 (High Stealth)[/bold yellow]\n"
        "Classification: [bold white]Encrypted-Primary / Low-Visibility[/bold white]"
    )
    
    return analysis_text
